# 🤖 RustAssistant — LLM AUDIT REPORT Report

> Auto-generated by [RustAssistant](https://github.com/nuniesmith/rustassistant/actions/runs/22803727184)

| | |
|---|---|
| **Repository** | `nuniesmith/futures` |
| **Branch** | `main` |
| **Mode** | `full` |
| **Provider** | Grok 4.1 Fast (xAI) |
| **Date** | 2026-03-07 17:31 UTC |
| **Run** | [#7](https://github.com/nuniesmith/rustassistant/actions/runs/22803727184) |

---

## 📋 Summary

The 'nuniesmith/futures' monorepo is a production-grade futures trading co-pilot featuring ML-driven breakout detection (87% acc CNN), FastAPI/HTMX dashboard, NinjaTrader 8 C# bridge, Docker orchestration, Redis/Postgres persistence, and full observability. Strengths include modular lib/services split, 30+ pytest suite, excellent docs, and CI/CD. Critical gaps: missing TPT 4PM hard flatten (account termination risk), unvalidated ONNX/PyTorch parity, committed DLLs supply-chain vulnerability, and incomplete FastAPI validation hinder live prop trading readiness.

**Overall Health:** 87/100

---

## 🎯 Priority Actions

1. Implement TPT 4PM ET hard flatten in BreakoutStrategy.cs with risk block until 18:00 ET
1. Validate/automate ONNX ↔ PyTorch parity test in CI for v6 18-feature model
1. Add SHA256 checksum verification for committed DLLs in deploy_nt8.ps1
1. Add Pydantic models and rate limiting to all FastAPI data/api endpoints
1. Deploy/test NT8 full session: verify tabular dim=18, TP3 mults loaded, no OCO/signal errors

---

## 🔍 Findings

### HIGH: Missing TPT 4PM Hard Flatten

**Category:** quality

BreakoutStrategy.cs lacks 16:00 ET position flatten for Take Profit Trader compliance, risking overnight holds and account termination (todo.md critical).

**Recommendation:** Add OnBarUpdate() guard: if TptMode && ET hour>=16 && positions>0, FlattenAll('TPT_HARD_STOP'); unblock at 18:00 ET. Test sim/live.

**Files:** src/ninja/BreakoutStrategy.cs, todo.md

---

### HIGH: Unvalidated ONNX/PyTorch Inference Parity

**Category:** quality

Pending parity check on models/breakout_cnn_best.* for v6 18-features risks Python/NT8 divergence (todo.md).

**Recommendation:** Extend test_phase3_ema9_parity.py for batch inference; assert max abs diff <1e-4. Automate in CI pytest.

**Files:** src/tests/test_phase3_ema9_parity.py, models/breakout_cnn_best.onnx, models/breakout_cnn_best.pt, todo.md

---

### MEDIUM: Committed OnnxRuntime DLLs Without Checksums

**Category:** security

src/ninja/dll/ binaries risk tampering; deploy_nt8.ps1 lacks verification.

**Recommendation:** Add SHA256 checksums in deploy_nt8.ps1; verify before NT8 copy. Consider NuGet restore isolation.

**Files:** src/ninja/dll/, scripts/deploy_nt8.ps1, src/ninja/dll/README.md

---

### MEDIUM: FastAPI Endpoints Lack Pydantic/Rate Limiting

**Category:** security

data/api/*.py (positions, sar, trades) miss input validation and rate_limit, vulnerable to injection/malformed requests.

**Recommendation:** Add Pydantic BaseModels and Depends(rate_limit) to all POST/PUT/DELETE; audit SQLi/XSS.

**Files:** src/lib/services/data/api/*.py

---

### MEDIUM: Python+C# Monorepo Complexity

**Category:** architecture

Mixed langs; pyproject.toml ignores C#, manual csproj patches in deploy scripts complicate CI/testing.

**Recommendation:** Add dotnet format/lint/test to .github/workflows/ci-cd.yml; isolate NT8 build; consider ninja/ subrepo.

**Files:** src/ninja/, scripts/deploy_nt8.ps1, .github/workflows/ci-cd.yml, pyproject.toml

---

### MEDIUM: Potential API Key Exposure in Docs/Env

**Category:** security

README.md/.env.example placeholders; docker-compose.yml secrets lack rotation guidance.

**Recommendation:** Confirm .env in .gitignore; add GitHub secret scanning; rotate MASSIVE/XAI/KRAKEN keys quarterly.

**Files:** README.md, .env.example, docker-compose.yml

---

### LOW: Forex Tick Precision Bug in UI

**Category:** quality

6E asset cards collapse entry/stop/TP due to 4-decimal rounding vs 5-decimal tick (todo.md).

**Recommendation:** Use MICRO_CONTRACT_SPECS tick_size in web/main.py _compute_entry_zone() for dynamic precision.

**Files:** src/lib/services/web/main.py, todo.md

---

### LOW: No C# Unit Tests

**Category:** quality

Python 30+ tests; C# relies on manual NT8 Analyzer (no NUnit).

**Recommendation:** Add NUnit for range builders/phases/MTF; integrate dotnet test in CI.

**Files:** src/ninja/BreakoutStrategy.cs

---

### LOW: Duplicated Data Fetch Logic

**Category:** performance

Cache→DB→API repeated; no unified resolver for services/training.

**Recommendation:** Implement DataResolver in services/data/main.py: Redis→Postgres→Massive/Kraken with backfill.

**Files:** src/lib/services/data/main.py, src/lib/training/dataset_generator.py

---

### LOW: No Trading/Review UI Modes

**Category:** quality

Dashboard cluttered during trading; no toggle (todo.md).

**Recommendation:** Add localStorage toggle in web/main.py; CSS .mode-trading/.mode-review.

**Files:** src/lib/services/web/main.py, todo.md

---

## ✅ Strengths

- Comprehensive 30+ pytest suite (ORB, ICT, risk, scheduler, integrations, parity tests)
- Modular architecture: lib/analysis/core/services + feature_contract.json parity enforcement
- Production Docker Compose (7 services, healthchecks), Tailscale CI/CD with Discord notifies
- Full observability: structlog, Prometheus/Grafana (provisioned dashboards)
- Mature ML: 87.1% acc CNN (v6 18-features, ONNX export, 13 breakout types, balanced datasets)
- Excellent docs: README/todo/STRATEGY_PLAN + model sync/deploy scripts
- Monorepo consolidation: Python engine/web/trainer + C# NT8 + models/DLLs

## ⚠️ Weaknesses

- Mixed Python/C# increases CI/testing complexity; no C# linter/tests
- Heavy deps (torch GPU-optional, onnxruntime); ~80MB LFS bloat from models/DLLs
- UI noisy (Grok auto-poll, no trading/review modes); forex precision issues
- Duplicated data resolvers; Kraken training integration pending
- Incomplete FastAPI validation; committed binaries supply-chain risk
- TODO backlog (20+ items) despite clean code and many completions

## 📝 Technical Debt

- Trainer UI → dedicated /train page (API-only trainer_server)
- Settings page (/settings) for env toggles/API status/Tailscale IPs
- Kraken keys in CI secrets + full training/data resolver integration
- Cross-asset signals (crypto-futures correlation → CNN v7 feature)
- C# NUnit tests + dotnet lint/format in CI
- Per-type/session win-rate Grafana panels + feature contract drift detection

## ➡️ Next Steps

1. Run full tests/lint: pytest src/tests/ -x --cov && ruff check src/
2. Sync models: scripts/sync_models.sh --restart
3. NT8 deploy/test: scripts/deploy_nt8.bat -Launch; check logs (dim=18, TP3 mults)
4. Docker up monitoring: docker compose --profile monitoring up -d; verify localhost:8180
5. Fix high findings: TPT flatten, ONNX parity test, DLL checksums
6. Test Kraken training: add keys to .env/CI; run dataset_generator
7. Live NT8/Python side-by-side session; audit logs/performance
8. Add C# tests/DataResolver; retrain if parity fails


---

*Auto-generated — re-run for updated analysis.*
