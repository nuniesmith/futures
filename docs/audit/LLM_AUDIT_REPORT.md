# 🤖 RustAssistant — LLM AUDIT REPORT Report

> Auto-generated by [RustAssistant](https://github.com/nuniesmith/rustassistant/actions/runs/22791043831)

| | |
|---|---|
| **Repository** | `nuniesmith/futures` |
| **Branch** | `main` |
| **Mode** | `full` |
| **Provider** | Grok 4.1 Fast (xAI) |
| **Date** | 2026-03-07 03:25 UTC |
| **Run** | [#5](https://github.com/nuniesmith/rustassistant/actions/runs/22791043831) |

---

## 📋 Summary

The 'nuniesmith/futures' repository is a sophisticated monorepo for a futures trading co-pilot, integrating Python ML-driven breakout detection (87% acc CNN), FastAPI/HTMX dashboard, NinjaTrader 8 C# strategies via Bridge, Docker Compose deployment, Redis/Postgres storage, and Prometheus/Grafana monitoring. Extensive testing (30+ files), detailed docs, and CI/CD shine, but critical TPT compliance (4PM hard flatten), unvalidated ONNX/PyTorch parity, and minor UI/security gaps prevent full production readiness. Overall, high-quality code with focused fixes needed for live prop trading.

**Overall Health:** 90/100

---

## 🎯 Priority Actions

1. Implement TPT 4:00 PM ET hard flatten in BreakoutStrategy.cs to ensure no overnight positions
1. Validate ONNX/PyTorch inference parity on v6 18-feature model and automate in CI
1. Deploy and test NT8 full session with logs verification for tabular dim=18 and TP3 mults
1. Add input validation/rate limiting to all FastAPI endpoints and rotate API secrets

---

## 🔍 Findings

### HIGH: Missing TPT Hard Stop at 16:00 ET

**Category:** quality

BreakoutStrategy.cs lacks time-based flatten for Take Profit Trader accounts, risking overnight holds and account termination as noted in multiple TODOs.

**Recommendation:** Add OnBarUpdate() guard: if TptMode && hour>=16 && positions>0, FlattenAll('TPT_HARD_STOP'); reset RiskBlocked at 18:00 ET. Test in live session.

**Files:** src/ninja/BreakoutStrategy.cs, todo.md, docs/STRATEGY_PLAN.md

---

### HIGH: Unvalidated ONNX/PyTorch Model Parity

**Category:** quality

TODO pending parity check on committed models despite v6 18-feature contract; risks divergence between Python engine and NT8.

**Recommendation:** Extend test_phase3_ema9_parity.py for full tabular batch inference; assert max abs diff <1e-4. Run on champion model and add to CI.

**Files:** src/tests/test_phase3_ema9_parity.py, models/breakout_cnn_best.onnx, models/breakout_cnn_best.pt, todo.md

---

### MEDIUM: Potential API Key Exposure in Docs/Env

**Category:** security

README/.env.example show placeholders; CI/CD secrets unconfirmed. No committed .env but risk if overlooked.

**Recommendation:** Verify .env in .gitignore; use GitHub secrets for CI/CD; add secret scanning in CI; scrub history if any keys committed.

**Files:** README.md, .env.example, .github/workflows/ci-cd.yml, docker-compose.yml

---

### MEDIUM: Monorepo Complexity from Python+C# Mix

**Category:** architecture

src/lib (Python) + src/ninja (C#/DLLs) in one repo; deploy scripts patch NT8 csproj, complicating branching/scaling.

**Recommendation:** Add C# linter (dotnet format) to CI; consider extracting ninja/ to separate repo if NT8 independent. Document build isolation.

**Files:** src/ninja/, scripts/deploy_nt8.ps1, pyproject.toml

---

### MEDIUM: Committed DLLs Without Checksum Verification

**Category:** security

src/ninja/dll/ OnnxRuntime binaries committed; supply-chain tampering risk without validation.

**Recommendation:** Add SHA256 checksums in deploy_nt8.ps1; verify before copy. Prefer NuGet if NT8 supports.

**Files:** src/ninja/dll/, scripts/deploy_nt8.ps1

---

### LOW: Forex Tick Precision Bug in UI Asset Cards

**Category:** quality

Low-ATR forex (6E) shows collapsed entry/stop/TP due to 4-decimal rounding vs 5-decimal tick size.

**Recommendation:** In src/lib/services/web/main.py _compute_entry_zone(), use symbol-specific decimals from MICRO_CONTRACT_SPECS or ATR-based precision.

**Files:** todo.md, src/lib/services/web/main.py

---

### LOW: No Unified DataResolver Across Services

**Category:** performance

Cache→DB→API fetch duplicated; training lacks cache-first Kraken integration.

**Recommendation:** Implement DataResolver class for Redis→Postgres→Massive/Kraken with auto-backfill; use in engine/training.

**Files:** src/lib/services/data/main.py, src/lib/training/dataset_generator.py

---

### LOW: No C# Unit Tests

**Category:** quality

Python has 30+ tests; C# relies on manual NT8 Analyzer.

**Recommendation:** Add NUnit tests for BreakoutStrategy.cs key logic (range builders, phases); integrate to CI via dotnet test.

**Files:** src/ninja/BreakoutStrategy.cs

---

### LOW: Missing Trading/Review UI Modes

**Category:** quality

Dashboard cluttered during trading; no toggle for low-latency vs analysis views.

**Recommendation:** Add header toggle storing in localStorage; CSS .mode-trading/.mode-review to hide panels.

**Files:** todo.md, src/lib/services/web/main.py

---

### LOW: FastAPI Endpoints Lack Input Validation

**Category:** security

data/api/* routers exposed without Pydantic validation/rate limits on all.

**Recommendation:** Add Depends(rate_limit) and strict models to all POST/PUT; audit for injection.

**Files:** src/lib/services/data/api/*.py

---

## ✅ Strengths

- Comprehensive pytest suite (30+ files: ORB, ICT, risk, scheduler, integrations, parity)
- Modular lib: analysis/core/services cleanly separated; feature_contract.json v6 enforces parity
- Production Docker Compose (7 services w/ healthchecks), Tailscale CI/CD w/ Discord notifies
- Observability: structlog, Prometheus/Grafana (training health dashboard provisioned)
- Mature ML: 87.1% acc champion, ONNX export, balanced dataset gen, 13 breakout types
- Excellent docs: README/todo.md/STRATEGY_PLAN.md; monorepo consolidation complete

## ⚠️ Weaknesses

- Heavy deps (torch optional-GPU, onnxruntime); no pyproject lockfile
- C# lacks tests/formatting; manual NT8 validation
- UI noisy (Grok auto-poll, no modes); forex precision bug
- Models/DLLs bloat repo (~80MB despite LFS)
- Duplicated data fetch logic; Kraken training integration pending

## 📝 Technical Debt

- Trainer UI → /train page (API-only trainer_server)
- Settings page (/settings) for toggles/API status
- Kraken keys in CI secrets + full training integration
- Cross-asset signals (crypto → futures)
- Per-type/session win-rate Grafana panels
- Feature contract drift detection in C# loader

## ➡️ Next Steps

1. Run tests/lint: pytest src/tests/ && ruff check src/
2. Sync models: scripts/sync_models.sh --restart
3. NT8 deploy: scripts/deploy_nt8.bat -Launch; check logs (dim=18, TP3 mults)
4. Live test: docker compose --profile monitoring up; monitor localhost:8180
5. Fix priorities: TPT stop, parity test, forex UI
6. Retrigger training if needed: scripts/smoke_test_trainer.py
7. Add C# tests and DataResolver


---

*Auto-generated — re-run for updated analysis.*
